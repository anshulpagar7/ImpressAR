# Copy to .env (or export in your shell) before `npm run api`.
# Only ONE of these is needed. Gemini is FREE (aistudio.google.com → Get API key).
GEMINI_API_KEY=AIza...
# ANTHROPIC_API_KEY=sk-ant-...   # optional paid alternative
